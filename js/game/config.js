/** Base width of the in-game canvas in CSS-independent pixels. */
export const baseCanvasWidth = 720;

/** Base height of the in-game canvas in CSS-independent pixels. */
export const baseCanvasHeight = 480;

/** Duration of the intro-to-start-screen transition in milliseconds. */
export const introTransitionDuration = 700;

/** @type {string[]} Dialog ids that participate in the game menu flow. */
export const gameMenuDialogIds = [
  'gameMenuDialog',
  'settingsDialog',
  'instructionsDialog',
  'impressumDialog',
  'datenschutzDialog',
  'creditsDialog',
];